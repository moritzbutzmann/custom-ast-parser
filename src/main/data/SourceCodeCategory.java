package main.data;

/**
 * Enum to differentiate between the different types of source code needed for analysis.
 */
public enum SourceCodeCategory {
    manual_nontest, manual_test, generated_nontest, generated_test
}
